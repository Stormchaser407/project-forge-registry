#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
REPO_ROOT=$(CDPATH= cd -- "$SCRIPT_DIR/.." && pwd)

export PYTHONPATH="${PYTHONPATH:-}:$REPO_ROOT/src"

exec python3 -m project_forge_registry "$@"
